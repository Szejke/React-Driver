import React from 'react';
import styled from 'styled-components/macro';

const StyledWrapper = styled.div`
  margin-top: 20px;
`;

const Home = () => {
  return <StyledWrapper>asds</StyledWrapper>;
};

export default Home;
