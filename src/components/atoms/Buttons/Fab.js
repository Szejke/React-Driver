import styled from 'styled-components';
import ButtonBootstrap from 'react-bootstrap/Button';

const Fab = styled(ButtonBootstrap)`
  width: 60px;
  border-radius: 50%;
  font-weight: 600;
  font-size: 30px;
`;

export default Fab;
