import styled from 'styled-components';
import ButtonBootstrap from 'react-bootstrap/Button';

const IconButton = styled(ButtonBootstrap)`
  background-color: unset;
  color: unset;
  margin: 0.4rem;
`;

export default IconButton;
